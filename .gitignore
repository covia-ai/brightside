# Build output
/target/

# IDE / editor
/.idea/
/*.iml
/.vscode/
/.settings/
/.project
/.classpath
/.factorypath

# Agent tooling (settings.json is shared; everything else is personal).
# Codex's project layer (.codex/config.toml, hooks.json, rules/) is shared by
# design — personal Codex state lives in ~/.codex — so it is not ignored.
CLAUDE.local.md
/.claude/*
!/.claude/README.md
!/.claude/settings.json
!/.claude/skills/

# Claude Code's MCP server for the running venue carries a bearer token
# (docs/CONFIGURATION.md, "From Claude Code") — never commit it.
/.mcp.json

# Local scratch: personal configs, stores, keys, logs
/dev/
*.etch
*.key
*.log
