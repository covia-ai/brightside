#
# Brightside's light-theme defaults (loaded after FlatLaf.properties for the
# FlatLightLaf family, which includes the macOS light theme).
#

# An elevated surface a step darker than the panel: assistant bubbles, cards,
# chips. The dark theme steps lighter instead (FlatDarkLaf.properties).
Brightside.surface = shade(@background,6%)
