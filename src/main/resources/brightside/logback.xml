<configuration>
	<!-- BrightSide logging: console plus a rolling file under the active app home.
	     Loaded programmatically by BrightSide.configureLogging(), so it wins
	     regardless of which dependency jars also ship a logback.xml. -->
	<statusListener class="ch.qos.logback.core.status.NopStatusListener" />

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%date{HH:mm:ss.SSS} %-5level %logger{30} - %msg%n</pattern>
		</encoder>
	</appender>

	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${brightside.home}/logs/brightside.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<fileNamePattern>${brightside.home}/logs/brightside.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
			<maxFileSize>10MB</maxFileSize>
			<maxHistory>7</maxHistory>
			<totalSizeCap>100MB</totalSizeCap>
		</rollingPolicy>
		<encoder>
			<pattern>%date{yyyy-MM-dd'T'HH:mm:ss.SSSX, UTC} %-5level [%thread] %logger{36} - %msg%n</pattern>
		</encoder>
	</appender>

	<root level="INFO">
		<appender-ref ref="FILE" />
		<appender-ref ref="STDOUT" />
	</root>

	<logger name="io.javalin" level="WARN" />
	<logger name="org.eclipse.jetty" level="WARN" />
	<logger name="dev.langchain4j" level="WARN" />
	<!-- Spurious noise from the MCP SDK's reactive plumbing (as in covia.jar) -->
	<logger name="io.modelcontextprotocol" level="OFF" />
	<logger name="reactor.core.publisher" level="OFF" />
</configuration>
