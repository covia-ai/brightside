{
	"name": "Shut down Brightside",
	"description": "Cleanly shuts down this Brightside desktop process — flushes the venue store and exits — so a newly launched instance can take over. Venue-operator only: the caller must authenticate as the venue's own DID (a venue-signed token).",
	"operation": {
		"adapter": "brightside:shutdown",
		"input": {
			"type": "object",
			"properties": {}
		},
		"output": {
			"type": "object",
			"properties": {
				"accepted": { "type": "boolean", "description": "True when the operator was authorised and shutdown was scheduled." }
			}
		}
	}
}
